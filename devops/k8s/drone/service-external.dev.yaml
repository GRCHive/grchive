apiVersion: v1
kind: Service
metadata:
    name: external-drone-service
    namespace: backend
    labels:
        app: drone
spec:
    ports:
        - port: 8888
          protocol: TCP
    type: NodePort
    selector:
        app: drone
