apiVersion: v1
kind: Service
metadata:
    name: internal-vault-service.grchive.com
    labels:
        app: vault
spec:
    ports:
        - port: 8200
          protocol: TCP
    selector:
        app: vault
