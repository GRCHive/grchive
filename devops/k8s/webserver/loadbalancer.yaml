apiVersion: v1
kind: Service
metadata:
    name: external-webserver-service
    labels:
        app: webserver
spec:
    ports:
        - port: 80
          protocol: TCP
          targetPort: 8080
    type: LoadBalancer
    selector:
        app: webserver
