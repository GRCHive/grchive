interface BoundingBox {
    x: number,
    y: number,
    width: number,
    height: number
}

interface TransformData {
    tx: number,
    ty: number
}

interface Point2D {
    x: number,
    y: number
}
